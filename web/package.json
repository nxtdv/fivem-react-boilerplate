{
  "name": "fivem-react-boilerplate",
  "version": "0.1.0",
  "homepage": "web/build",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "sass": "^1.51.0"
  },
  "scripts": {
    "start": "cross-env PUBLIC_URL=/ craco start",
    "start:game": "cross-env IN_GAME_DEV=1 craco start",
    "build": "rimraf build && craco build",
    "test": "react-scripts test"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "@craco/craco": "^6.4.3",
    "rimraf": "^3.0.2"
  }
}